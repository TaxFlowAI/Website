// Page uses search params (fn, ln, em, ph) — render at request time
export const dynamic = "force-dynamic";

export default function FreeEligibilityTestHomeLoansLayout({ children }) {
  return children;
}
