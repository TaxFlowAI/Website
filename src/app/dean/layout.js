export const metadata = {
  title: "Dean Tinellis — Partner at Frontline Financial",
  description:
    "An update from Dean Tinellis on his new role as Partner at Frontline Financial.",
  robots: {
    index: false,
    follow: false,
    nocache: true,
    googleBot: {
      index: false,
      follow: false,
    },
  },
};

export default function DeanLayout({ children }) {
  return children;
}
